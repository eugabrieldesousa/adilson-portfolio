---
import type { ImageMetadata } from 'astro';
import { Image } from 'astro:assets';
import { perfil } from '../dados/perfil';
import Botao from '../componentes/base/Botao.astro';

const imagens = import.meta.glob<{ default: ImageMetadata }>('/src/assets/imagens/*.{jpg,jpeg,png,webp,avif}', {
  eager: true
});

const chaveRetrato = perfil.retratoHeroArquivo
  ? `/src/assets/imagens/${perfil.retratoHeroArquivo}`
  : '';
const retrato = chaveRetrato ? imagens[chaveRetrato]?.default : undefined;
---

<section id="home" class="hero" aria-labelledby="hero-title">
  <div class="hero__interno">
    <div class="hero__texto" data-aos="fade-up" data-aos-delay="80">
      <p class="hero__marcador">Portfolio &amp; Product Development Leader | Engineer | Expert in Materials Science</p>
      <h1 id="hero-title">{perfil.nome}</h1>
      <p class="hero__titulo">{perfil.titulo}</p>
      <p class="hero__resumo">{perfil.resumoHero}</p>
      <div class="hero__acoes" aria-label="Primary actions">
        <Botao href="#experience" variante="primario">Explore Experience</Botao>
        <Botao href="#projects" variante="fantasma">View Selected Work</Botao>
      </div>
      <div class="hero__continuidade" aria-hidden="true"></div>
    </div>

    <div class="hero__visual" data-aos="fade-up" data-aos-delay="180">
      <div class="hero__retrato">
        {retrato ? (
          <Image
            src={retrato}
            alt={perfil.retratoAlt}
            loading="eager"
            decoding="async"
            widths={[320, 480, 640, 760]}
            sizes="(min-width: 1024px) 420px, (min-width: 768px) 380px, 82vw"
            class="hero__imagem"
          />
        ) : (
          <div class="hero__retrato-vazio" aria-hidden="true"></div>
        )}
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    display: grid;
    min-height: 100vh;
    min-height: 100svh;
    align-items: center;
    padding: var(--espaco-10) var(--espaco-5) calc(var(--altura-nav) + var(--espaco-6) + env(safe-area-inset-bottom));
    overflow: hidden;
  }

  .hero__interno {
    display: grid;
    width: min(100%, 1260px);
    align-items: center;
    margin-inline: auto;
    gap: var(--espaco-8);
  }

  .hero__texto {
    min-width: 0;
    max-width: 680px;
  }

  .hero__marcador {
    max-width: 34ch;
    margin: 0 0 var(--espaco-5);
    color: var(--cor-texto-minimo);
    font-size: 0.74rem;
    font-weight: 600;
    line-height: 1.45;
    text-transform: uppercase;
  }

  h1 {
    margin: 0;
    font-family: var(--fonte-display);
    max-width: 7.5ch;
    font-size: 3.05rem;
    font-weight: 680;
    line-height: 0.92;
    letter-spacing: 0;
    text-wrap: balance;
  }

  .hero__titulo {
    margin: var(--espaco-6) 0 0;
    color: var(--cor-texto);
    font-size: 1.08rem;
    font-weight: 620;
    line-height: 1.35;
    overflow-wrap: break-word;
  }

  .hero__resumo {
    max-width: 54ch;
    margin: var(--espaco-4) 0 0;
    color: var(--cor-texto-suave);
    font-size: 1rem;
    line-height: 1.6;
    overflow-wrap: break-word;
  }

  .hero__acoes {
    display: flex;
    flex-direction: column;
    flex-wrap: wrap;
    align-items: flex-start;
    gap: var(--espaco-3);
    margin-top: var(--espaco-6);
  }

  .hero__acoes :global(.botao) {
    width: min(100%, 240px);
  }

  .hero__continuidade {
    width: 72px;
    height: 1px;
    margin-top: var(--espaco-6);
    background: linear-gradient(90deg, var(--cor-acento), transparent);
  }

  .hero__visual {
    display: flex;
    justify-content: center;
  }

  .hero__retrato {
    position: relative;
    width: min(58vw, 200px);
    z-index: 0;
    aspect-ratio: 4 / 5;
    overflow: hidden;
    border: 0;
    border-radius: 42% 58% 48% 52% / 34% 38% 62% 66%;
    background: transparent;
    box-shadow: var(--sombra-retrato);
  }

  .hero__retrato::after {
    position: absolute;
    inset: 0;
    z-index: 2;
    content: "";
    background:
      linear-gradient(135deg, rgba(255, 255, 255, 0.22), transparent 28%),
      linear-gradient(315deg, rgba(0, 102, 204, 0.08), transparent 34%);
    mix-blend-mode: screen;
    pointer-events: none;
  }

  .hero__imagem {
    width: 100%;
    height: auto;
    position: relative;
    z-index: 1;
    object-fit: cover;
    object-position: 50% 20%;
    mix-blend-mode: multiply;
  }

  .hero__retrato-vazio {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(30deg, transparent 47%, var(--cor-borda-suave) 48%, var(--cor-borda-suave) 49%, transparent 50%),
      linear-gradient(150deg, transparent 47%, var(--cor-borda-suave) 48%, var(--cor-borda-suave) 49%, transparent 50%),
      var(--cor-superficie-forte);
    background-size: 96px 56px;
  }

  .hero__retrato-vazio::after {
    position: absolute;
    inset: var(--espaco-5);
    content: "";
    border: 1px solid var(--cor-borda);
    border-radius: 18px;
  }

  @media (min-width: 768px) {
    .hero {
      padding-inline: var(--espaco-8);
    }

    .hero__interno {
      grid-template-columns: minmax(0, 1.15fr) minmax(260px, 0.85fr);
      justify-content: space-between;
      gap: var(--espaco-8);
    }

    .hero__texto {
      max-width: 500px;
    }

    h1 {
      max-width: 7ch;
      font-size: clamp(4.35rem, 9vw, 5.2rem);
    }

    .hero__titulo {
      font-size: 1.25rem;
    }

    .hero__resumo {
      font-size: 1.08rem;
    }

    .hero__marcador {
      max-width: 100%;
      font-size: 0.78rem;
    }

    .hero__acoes {
      flex-direction: row;
      align-items: center;
    }

    .hero__acoes :global(.botao) {
      width: auto;
    }

    .hero__retrato {
      width: min(100%, 320px);
    }
  }

  @media (min-width: 1024px) {
    .hero__interno {
      grid-template-columns: minmax(520px, 640px) minmax(330px, 390px);
      align-items: center;
      justify-content: center;
      gap: clamp(var(--espaco-10), 4vw, var(--espaco-16));
    }

    h1 {
      max-width: 7.4ch;
      font-size: clamp(5.1rem, 6.1vw, 5.9rem);
    }

    .hero__visual {
      justify-content: flex-end;
    }

    .hero__retrato {
      width: min(100%, 390px);
    }
  }

  @media (min-width: 1280px) {
    .hero__marcador {
      white-space: nowrap;
    }

    h1 {
      max-width: none;
      font-size: clamp(5rem, 5.35vw, 5.75rem);
      white-space: nowrap;
    }

    .hero__titulo {
      max-width: 30ch;
    }

    .hero__resumo {
      max-width: 60ch;
    }
  }

  @media (max-width: 420px) {
    .hero__interno {
      gap: var(--espaco-2);
    }

    .hero__retrato {
      width: min(48vw, 160px);
    }
  }
</style>
